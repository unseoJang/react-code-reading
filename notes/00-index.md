# React 코드 까보기 질문 목록

## 1단계
- JSX는 언제 React Element가 되는가?
- <App />은 언제 App 함수로 실행되는가?
- createRoot는 뭘 만드는가?
- root.render는 바로 DOM을 바꾸는가?

## 2단계
- Fiber는 왜 필요한가?
- useState 값은 어디에 저장되는가?
- setState는 즉시 실행인가, 예약인가?

## 3단계
- render phase와 commit phase는 뭐가 다른가?
- useEffect는 왜 렌더 후 실행되는가?

---

# React 코드 까보기 질문 지도

## 1단계: React 렌더링 입구

- [x] `<App />`은 언제 App 함수로 실행되는가?
- [ ] JSX는 언제 React Element가 되는가?
- [ ] `createRoot`는 뭘 만드는가?
- [ ] `root.render`는 바로 DOM을 바꾸는가?

## 2단계: React 내부 구조

- [ ] Fiber는 왜 필요한가?
- [ ] `useState` 값은 어디에 저장되는가?
- [ ] `setState`는 즉시 실행인가, 예약인가?

## 3단계: 렌더링 단계

- [ ] render phase와 commit phase는 뭐가 다른가?
- [ ] `useEffect`는 왜 렌더 후 실행되는가?